{
  "name": "meetsync-php",
  "version": "2.0.0",
  "description": "MeetSync - Pure PHP Application",
  "scripts": {
    "build": "echo 'No build needed - Pure PHP application' && exit 0",
    "dev": "echo 'Use PHP built-in server or Apache' && exit 0"
  }
}
